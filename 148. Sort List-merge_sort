/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode(int x) : val(x), next(NULL) {}
 * };
 */
class Solution {

public:

ListNode* Merge(ListNode*elift,right)
    ListNode* newhead=new ListNode(0);
    ListNode *t=newhead;
    while(le>val<rig
          }{
              tm-<bex=-jeflen0
                  else
                  {
elsie
                  lskrp0
                  n0=
if)ilr+
    rep-m;iu
                  return newd->nextl}
          })
        ListnLKjsidkifhioonihhasifwhafoafiporehjinvin-niuol
        ljoslifirurtn merge(sdjdosirs)
